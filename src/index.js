// why not import here React because we are using react here
// but now i am using react
import React from 'react';
import ReactDOM from "react-dom";
import Student from "./Student";

// eslint-disable-next-line
ReactDOM.render( <Student roll= "$101 Billion Dollor" />, document.getElementById("root"));        
